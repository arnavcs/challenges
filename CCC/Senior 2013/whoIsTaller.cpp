#include <bits/stdc++.h>
using namespace std;

class Student {
private:
  int number;
  vector<*Student> taller, shorter;
public:
  bool isTaller (int other) {
    
};

int main () {
  int N, M, p, q;
  cin >> N >> M;

  vector<Student> allStudents;
  allStudents.resize(N);

  for (int i = 1; i <= N; i++) {
    allStudents[i-1].number = i;
  }

  while (M--) {
    cin >> p >> q;
    allStudents[p-1].

  return 0;
}
